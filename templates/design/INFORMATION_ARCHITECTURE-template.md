# Information architecture — {{FEATURE}} ({{PROJECT}})

> Only when the surface has its own navigation/flow. Lives in
> `docs/design/<feature>/IA.md`, next to the brief.

## View map

| View | URL | Purpose |
|---|---|---|
| {{}} | {{}} | {{}} |

## Hierarchy per view

{{For each view: what dominates, what supports, what is progressive
disclosure. Content-first: name the real blocks, not "a section".}}

## Flows (WITH error paths)

{{Happy path + every error/empty/loading branch the user can hit. A flow
without its error path is half a flow.}}

## What grows vs what is fixed

{{Which parts scale with content volume (lists, media) and which are
structural — decides layout strategy before any mockup.}}
