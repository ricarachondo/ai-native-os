# Team learnings (evergreen, never pruned)

> Append-only record. Unlike `MEMORY.md` (working state, gets pruned) this
> is NEVER deleted — it is the institutional memory of the process. Each
> entry: what happened, what we do differently now. Only learnings that
> change a concrete behavior go in — no generic retro-theater.
>
> At every sprint close: if a learning is project-agnostic (process, not
> stack), also propose it to the ai-native-os kit repo so the next project
> starts with it.

## Thematic index (just-in-time reading — do NOT read the whole file)

> Role agents: read ONLY the sections relevant to your task. Maintenance
> rule: whoever adds an entry updates this index in the same edit.

<!-- - **{Topic}** ({subtopics}): {sprints/dates where it lives} -->

<!-- ## YYYY-MM-DD — Sprint N (name)
**What happened**: ...
**What we do differently now**: ... -->
